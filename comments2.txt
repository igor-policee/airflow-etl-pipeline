Привет! Прежде всего хочу поблагодарить за действительно ценные советы, которые помогли решить задачу лучше.
Комментарии по проекту:

- Я добавил логи по ходу выполнения Python-кода. Хочется еще добавить логи в SQL-скрипты, но полагаю что не получится.

- Я переписал пайплайн с целью переноса максимума работы на SQL. К сожалению, от загрузки .csv на машину не получается отказаться.
При попытке сделать "copy from" с внешней ссылки, возвращается ошибка:

[2022-07-15 17:37:24] [58P01] ERROR: could not open file "https://storage.yandexcloud.net/s3-sprint3/cohort_3/igor_polishchuk/project/MjAyMi0wNy0wOFQwMDowMDowMAlUV3BCZVUxcE1IZE9lVEI0VGxaUmVFNUViM2hPUkc5M1RWRnNjRm95T1hsWU0wSjJZa2RzZW1GSFRtOWtWM005/user_orders_log_inc.csv" for reading: No such file or directory
[2022-07-15 17:37:24] Hint: COPY FROM instructs the PostgreSQL server process to read a file. You may want a client-side facility such as psql's \copy.

Также я пробовал с помощью интерфейса "psql \copy" - результат тот же. При этом ссылка рабочая и файл можно скачать.
Поэтому в текущей версии пайплайн подразумевает загрузку файла .csv на машину и его удаление после загрузки в базу данных.

- Еще возникла неожиданная проблема: .csv файл может содержать или нет поле "status" и для загрузки разных версий нужны разные стейджинг-таблицы.
Я надеялся, что можно будет обойтись одной (поле "status" будет null, если отсутствует в источнике), но "copy from" упорно возвращает ошибку, если не совпадает количество полей в источнике и целевой таблице.
Не удалось это побороть (а жаль, так-как с помощью UI IDE можно загружать файлы таким образом, используя только одну таблицу).

